import * as styles from "./styles";

const Link = ({ text, target }) => {
  const test = "test";
  return (
    <styles.LinkContainer>
      <p>{text}</p>
    </styles.LinkContainer>
  );
};

export default Link;
